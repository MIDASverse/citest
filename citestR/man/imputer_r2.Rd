% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{imputer_r2}
\alias{imputer_r2}
\title{Estimate imputer out-of-sample R-squared}
\usage{
imputer_r2(model_id, mask_frac = 0.2, m_eval = 1L, ...)
}
\arguments{
\item{model_id}{Character. UUID returned by \code{\link[=ci_test]{ci_test()}}.}

\item{mask_frac}{Numeric. Fraction of observed cells to hold out (default 0.2).}

\item{m_eval}{Integer. Number of imputations to average over (default 1).}

\item{...}{Arguments forwarded to \code{\link[=ensure_server]{ensure_server()}}.}
}
\value{
A list with \code{mean_r2} and \code{per_variable} (named numeric vector).
}
\description{
Runs a mask-and-impute diagnostic on the server.
}
\examples{
\dontshow{if (citestR:::has_server()) withAutoprint(\{ # examplesIf}
result <- ci_test(df, y = "Y")
imputer_r2(result$model_id)
\dontshow{\}) # examplesIf}
}
