% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{ci_test}
\alias{ci_test}
\title{Run the conditional independence test}
\usage{
ci_test(
  data,
  y,
  expl_vars = NULL,
  onehot = TRUE,
  imputer = "midas",
  classifier = "rf",
  m = 10L,
  n_folds = 10L,
  classifier_args = list(),
  imputer_args = list(),
  random_state = 42L,
  target_level = "variable",
  variance_method = "mi_crossfit",
  subsample_cap = 2000L,
  ...
)
}
\arguments{
\item{data}{A data frame (may contain \code{NA}).}

\item{y}{Character. Name of the outcome variable.}

\item{expl_vars}{Character vector of explanatory variable names, or \code{NULL}.}

\item{onehot}{Logical. One-hot encode categoricals (default \code{TRUE}).}

\item{imputer}{Character. Imputer backend: \code{"midas"} (default),
\code{"iterative"}, \code{"iterative2"}, \code{"complete"}, or \code{"null"}.}

\item{classifier}{Character. Classifier backend: \code{"rf"} (default),
\code{"et"}, or \code{"logistic"}.}

\item{m}{Integer. Number of multiply-imputed datasets (default 10).}

\item{n_folds}{Integer. Number of cross-validation folds (default 10).}

\item{classifier_args}{Named list of extra classifier arguments.}

\item{imputer_args}{Named list of extra imputer arguments.}

\item{random_state}{Integer. Random seed (default 42).}

\item{target_level}{Character. \code{"variable"} or \code{"column"}.}

\item{variance_method}{Character. \code{"mi_crossfit"} or \code{"legacy_fold"}.}

\item{subsample_cap}{Integer or \code{NULL}. Maximum rows to subsample.}

\item{...}{Arguments forwarded to \code{\link[=ensure_server]{ensure_server()}}.}
}
\value{
A list with elements \code{model_id}, \code{dataset_id}, and \code{results}.
The \code{results} element contains \code{m}, \code{B}, \code{W_bar}, \code{T}, \code{t_k}, \code{p_k},
\code{p_2s}, and optionally \code{df}.
}
\description{
All-in-one convenience function: creates a dataset on the server, builds a
\code{CIMissTest}, runs it, and returns the results.
}
\examples{
\dontshow{if (citestR:::has_server()) withAutoprint(\{ # examplesIf}
df <- data.frame(Y = rnorm(200), X1 = rnorm(200), X2 = rnorm(200))
df$X1[sample(200, 20)] <- NA
result <- ci_test(df, y = "Y")
result$results$p_2s
\dontshow{\}) # examplesIf}
}
