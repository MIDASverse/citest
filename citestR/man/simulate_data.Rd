% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{simulate_data}
\alias{simulate_data}
\title{Generate a simulated dataset}
\usage{
simulate_data(
  dgp,
  n = 1000L,
  ci = TRUE,
  missing_mech = "linear",
  beta_y = NULL,
  mcar_prop = NULL,
  k = NULL,
  ...
)
}
\arguments{
\item{dgp}{Character. Name of the DGP (e.g. \code{"single_mar"}, \code{"adult"}).}

\item{n}{Integer. Number of observations.}

\item{ci}{Logical. Conditional independence holds (\code{TRUE}) or not.}

\item{missing_mech}{Character. Missingness mechanism (\code{"linear"} or \code{"xor"}).}

\item{beta_y}{Numeric or \code{NULL}. Outcome effect size (for DGPs that use it).}

\item{mcar_prop}{Numeric or \code{NULL}. Proportion of MCAR missingness.}

\item{k}{Integer or \code{NULL}. Number of columns (for the \code{adult} DGP).}

\item{...}{Arguments forwarded to \code{\link[=ensure_server]{ensure_server()}}.}
}
\value{
A list with \code{dataset_id}, \code{n}, \code{columns}, \code{pct_missing}.
}
\description{
Calls one of the built-in data-generating processes on the Python server.
}
\examples{
\dontshow{if (citestR:::has_server()) withAutoprint(\{ # examplesIf}
sim <- simulate_data("single_mar", n = 500, ci = TRUE)
\dontshow{\}) # examplesIf}
}
