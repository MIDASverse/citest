% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server.R
\name{start_server}
\alias{start_server}
\title{Start the citest API server}
\usage{
start_server(python = "python3", port = NULL, venv = NULL, max_wait = 120L)
}
\arguments{
\item{python}{Path to the Python interpreter (default \code{"python3"}).}

\item{port}{Port to bind to. If \code{NULL}, a free port is chosen automatically.}

\item{venv}{Path to a Python virtual environment.
If supplied, the interpreter is taken from \verb{<venv>/bin/python}
(or \verb{<venv>/Scripts/python.exe} on Windows).}

\item{max_wait}{Maximum number of 0.5-second polling attempts (default 120,
i.e. 60 seconds). The first launch may be slower due to Python import
caching.}
}
\value{
Invisibly returns the port number.
}
\description{
Launches \verb{python -m citest_api} as a background process and waits for the
\verb{/health} endpoint to respond.
}
\examples{
\dontshow{if (citestR:::has_server()) withAutoprint(\{ # examplesIf}
start_server()
start_server(venv = "~/.virtualenvs/citest_env")
\dontshow{\}) # examplesIf}
}
